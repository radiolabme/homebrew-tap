# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: your-tool

before:
  hooks:
    - go mod tidy

builds:
  - id: your-tool
    main: ./cmd/your-tool
    binary: your-tool
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - id: default
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - Merge pull request
      - Merge branch

release:
  github:
    owner: radiolabme
    name: your-repo
  draft: false
  prerelease: auto

brews:
  - name: your-tool
    ids:
      - default
    repository:
      owner: radiolabme
      name: homebrew-tap
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/radiolabme/your-repo"
    description: "Your tool description - keep it short and clear"
    license: "MIT"
    # Skip upload if token not set (allows local testing)
    skip_upload: "{{ if .Env.HOMEBREW_TAP_TOKEN }}false{{ else }}true{{ end }}"

    # Custom install script
    install: |
      bin.install "your-tool"

    # Test block - verify installation works
    test: |
      system "#{bin}/your-tool", "--help"

    # Dependencies (uncomment as needed)
    # dependencies:
    #   - name: git
    #   - name: go
    #     type: build
